doctype html
html
    head
        meta(charset='UTF-8')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        title Vista de Productos
        link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css')
    body
    .container.mt-3.jumbotron
        h2(style='color: blue') Listado de  Producto
        table.table.table-striped
            tr
                th Title 
                th Price
                th Thumbnail

                each p in productos
                    tr
                        td= p.title
                        td= p.price 
                        td: img(src=p.thumbnail , alt="")(style='width: 40px')
        
        a.btn.btn-info.mt-3.ml-5(href='/') Formulario